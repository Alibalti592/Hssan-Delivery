# This repo has no root-level environment variables yet — docker-compose.yml
# currently uses fixed dev values for local Postgres.
#
# Each app manages its own environment file:
#   backend/.env         — committed, safe defaults (Symfony convention)
#   backend/.env.local    — gitignored, your real local overrides
